import http from 'node:http';import fs from 'node:fs';import path from 'node:path';import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..',process.env.ATLAS_EXPORT_DIR||'out');const prefix='/the-medicine';
const mime={'.html':'text/html; charset=utf-8','.js':'application/javascript','.css':'text/css','.json':'application/json','.txt':'text/plain; charset=utf-8','.svg':'image/svg+xml','.png':'image/png','.woff2':'font/woff2','.gz':'application/gzip'};
http.createServer((req,res)=>{try{const url=new URL(req.url,'http://localhost');if(url.pathname===prefix){res.writeHead(302,{Location:prefix+'/'});res.end();return}if(!url.pathname.startsWith(prefix+'/')){res.writeHead(404);res.end();return}let target=path.resolve(root,'.'+decodeURIComponent(url.pathname.slice(prefix.length)));if(target!==root&&!target.startsWith(root+path.sep)){res.writeHead(403);res.end();return}if(fs.statSync(target).isDirectory())target=path.join(target,'index.html');const stat=fs.statSync(target);res.writeHead(200,{'Content-Type':mime[path.extname(target)]||'application/octet-stream','Content-Length':stat.size,'Cache-Control':'no-cache'});fs.createReadStream(target).pipe(res)}catch{res.writeHead(404);res.end('Not found')}}).listen(4181,'127.0.0.1',()=>console.log('Static export: http://127.0.0.1:4181/the-medicine/'));
