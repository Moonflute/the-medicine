import Link from "next/link";
import { ArrowLeft, ChevronRight, ExternalLink, List, TableProperties } from "lucide-react";
import { notFound } from "next/navigation";
import { ParentPageFab } from "@/components/parent-page-fab";
import { buildLabImgBloodPanels, buildLabImgGroups } from "@/lib/lab-img-groups";
import { getLabImgNotes, getLabImgToc, type DomainNote } from "@/lib/webdb";

const EXTERNAL_RESOURCES: Record<string, ReadonlyArray<readonly [string, string]>> = {
  "03 심전도": [["LITFL ECG Library", "https://litfl.com/ecg-library/"], ["ECGpedia", "https://en.ecgpedia.org/"], ["Dr. Smith's ECG Blog", "https://drsmithsecgblog.com/"]],
  "04 영상검사": [["Radiopaedia", "https://radiopaedia.org"], ["Radiology Assistant", "https://radiologyassistant.nl"], ["LearningRadiology", "https://learningradiology.com"], ["Radiology Masterclass", "https://www.radiologymasterclass.co.uk"]],
};
function ExternalResourceLinks({ rawTitle }: { rawTitle: string }) {
  const resources = EXTERNAL_RESOURCES[rawTitle];
  if (!resources) return null;
  return <section className="rounded-lg border border-teal-100 bg-teal-50/60 p-5 shadow-sm"><h2 className="text-xl font-semibold text-slate-950">추천 외부 학습 자료</h2><p className="mt-1 text-sm text-slate-600">새 탭에서 열립니다.</p><div className="mt-4 grid gap-2 sm:grid-cols-2">{resources.map(([name, href]) => <a key={href} href={href} target="_blank" rel="noopener noreferrer" className="flex items-center justify-between rounded-lg border border-teal-100 bg-white px-4 py-3 text-sm font-semibold text-slate-950 transition hover:border-teal-300"><span>{name}</span><ExternalLink className="h-4 w-4 shrink-0 text-teal-700" /></a>)}</div></section>;
}
export function generateStaticParams() { return buildLabImgGroups(getLabImgNotes(), getLabImgToc()).map((group) => ({ slug: group.slug })); }
const summaries: Record<string, string> = { "CBC·Differential·Platelet": "빈혈, 감염·염증, 혈소판 이상을 시작점으로 평가합니다.", Electrolytes: "체액 상태와 신경·근육, 심장 리듬에 관련된 전해질을 봅니다.", Renal: "콩팥 기능과 여과 상태를 평가합니다.", "Glucose metabolism": "현재 혈당과 장기 혈당 상태를 함께 봅니다.", "Liver·Pancreas": "간세포 손상, 담즙정체, 합성 기능 및 췌장 손상을 평가합니다.", "Inflammation marker": "염증·감염의 보조 지표를 확인합니다.", "Cardiac marker": "심근 손상과 심부전 관련 지표를 평가합니다.", Coagulation: "출혈·혈전 및 응고 경로를 평가합니다.", Hormones: "갑상샘, 부신, 생식 관련 호르몬을 확인합니다.", "Iron status": "철 저장량과 빈혈 해석에 도움을 줍니다.", Lipid: "죽상경화성 심혈관 위험과 중성지방을 평가합니다.", Immunology: "면역글로불린과 보체를 평가합니다.", "Blood gas·Perfusion": "산소화, 환기, 산-염기 및 조직 관류를 평가합니다.", "General tissue injury": "비특이적 세포·조직 손상 보조 지표입니다." };
function NoteLinks({ notes }: { notes: DomainNote[] }) { return <div className="grid gap-2 sm:grid-cols-2 xl:grid-cols-3">{notes.map((note) => <Link key={note.slug} href={`/lab-img/${note.slug}`} className="flex items-center justify-between rounded-lg border border-slate-200 bg-slate-50 px-4 py-3 transition hover:border-teal-300 hover:bg-white"><span className="pr-3 text-sm font-medium text-slate-950">{note.title}</span><ChevronRight className="h-4 w-4 shrink-0 text-slate-400" /></Link>)}</div>; }
function BloodPage({ group, notes }: { group: ReturnType<typeof buildLabImgGroups>[number]; notes: DomainNote[] }) { const panels = buildLabImgBloodPanels(notes); const all = notes.filter((note) => note.pathSegments[0] === group.rawTitle).sort((a, b) => a.title.localeCompare(b.title, "ko")); return <div className="space-y-6"><Link href="/lab-img" className="inline-flex items-center gap-2 rounded-full border border-slate-200 bg-white px-4 py-2 text-sm text-slate-700"><ArrowLeft className="h-4 w-4" />Lab &amp; Img로 돌아가기</Link><header className="rounded-lg border border-teal-100 bg-gradient-to-br from-teal-50 to-white p-6 shadow-sm sm:p-8"><div className="text-xs font-semibold uppercase tracking-wider text-teal-700">Lab &amp; Img / 혈액검사</div><h1 className="mt-3 text-4xl font-semibold text-slate-950">검사 종류별로 찾아보기</h1><p className="mt-3 max-w-3xl text-sm leading-6 text-slate-600">검사명을 정확히 몰라도 임상 목적과 해석 맥락으로 찾아볼 수 있습니다. 참고범위는 결과지를 대신하지 않으며, 검사실 기준과 환자 상태를 함께 확인하세요.</p><nav className="mt-5 flex flex-wrap gap-2"><a href="#panels" className="inline-flex items-center gap-2 rounded-full bg-teal-700 px-4 py-2 text-sm font-semibold text-white"><TableProperties className="h-4 w-4" />종류별 보기</a><a href="#reference-guide" className="inline-flex items-center gap-2 rounded-full border border-teal-200 bg-white px-4 py-2 text-sm font-semibold text-teal-800"><List className="h-4 w-4" />정상범위 안내</a><a href="#all-tests" className="rounded-full border border-slate-200 bg-white px-4 py-2 text-sm font-semibold text-slate-700">전체 A–Z</a></nav></header><section id="panels" className="space-y-4">{panels.map((panel) => <article key={panel.title} className="rounded-lg border border-slate-200 border-l-4 border-l-teal-600 bg-white/85 p-5 shadow-sm"><div className="flex flex-wrap items-start justify-between gap-4"><div><h2 className="text-xl font-semibold text-slate-950">{panel.title}</h2><p className="mt-1 text-sm leading-6 text-slate-600">{summaries[panel.title] ?? "관련 검사를 함께 확인합니다."}</p></div><span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold text-slate-600">{panel.notes.length}개 검사</span></div>{panel.overviewNote ? <Link href={`/lab-img/${panel.overviewNote.slug}`} className="mt-4 inline-flex rounded-full border border-teal-200 bg-teal-50 px-4 py-2 text-sm font-semibold text-teal-800 hover:bg-white">{panel.title} 해설 보기</Link> : null}<div className="mt-3"><NoteLinks notes={panel.notes} /></div></article>)}</section><section id="reference-guide" className="scroll-mt-6 rounded-lg border border-teal-100 bg-teal-50/60 p-5 sm:p-6"><h2 className="text-2xl font-semibold text-slate-950">정상범위 안내</h2><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-700">정상범위는 빠른 참고용입니다. 실제 해석은 해당 검사실의 reference interval, 검체 상태, 성별·연령·임신 여부와 변화 추세를 우선합니다.</p><div className="mt-4 grid gap-2 sm:grid-cols-2 lg:grid-cols-3">{panels.filter((panel) => panel.overviewNote).map((panel) => <Link key={panel.title} href={`/lab-img/${panel.overviewNote!.slug}`} className="flex items-center justify-between rounded-lg border border-teal-100 bg-white px-4 py-3 text-sm font-semibold text-teal-900 hover:border-teal-300"><span>{panel.title} 참고범위</span><ChevronRight className="h-4 w-4" /></Link>)}</div></section><details id="all-tests" className="scroll-mt-6 rounded-lg border border-slate-200 bg-white/85 p-5 shadow-sm"><summary className="cursor-pointer text-lg font-semibold text-slate-950">전체 A–Z 보기 ({all.length}개)</summary><p className="mt-2 text-sm text-slate-600">검사명을 알고 있을 때 빠르게 찾는 보조 목록입니다. overview 및 index 문서는 별도로 표시됩니다.</p><div className="mt-4"><NoteLinks notes={all} /></div></details><ParentPageFab href="/lab-img" /></div>; }
export default async function LabImgCategoryPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const notes = getLabImgNotes(); const group = buildLabImgGroups(notes, getLabImgToc()).find((item) => item.slug === slug); if (!group) notFound(); if (group.rawTitle === "01 혈액검사") return <BloodPage group={group} notes={notes} />; return <div className="space-y-6"><Link href="/lab-img" className="inline-flex items-center gap-2 rounded-full border border-slate-200 bg-white px-4 py-2 text-sm text-slate-700"><ArrowLeft className="h-4 w-4" />Back to lab categories</Link><header className="rounded-lg border border-slate-200 bg-white/80 p-6 shadow-sm"><div className="text-xs uppercase text-slate-500">Lab &amp; Img</div><h1 className="mt-3 text-4xl font-semibold">{group.title}</h1></header><ExternalResourceLinks rawTitle={group.rawTitle} />{group.overviewNote ? <Link href={`/lab-img/${group.overviewNote.slug}`} className="inline-flex rounded-full border border-teal-200 bg-teal-50 px-4 py-2 text-sm font-semibold text-teal-800">{group.title} overview</Link> : null}{group.directNotes.length ? <NoteLinks notes={group.directNotes} /> : null}{group.childGroups.map((child) => <section key={child.rawTitle} className="rounded-lg border border-slate-200 bg-white/85 p-5 shadow-sm"><h2 className="mb-4 border-l-4 border-teal-600 pl-3 text-lg font-semibold">{child.title}</h2>{child.overviewNote ? <Link href={`/lab-img/${child.overviewNote.slug}`} className="mb-3 inline-flex rounded-full border border-teal-200 bg-teal-50 px-4 py-2 text-sm font-semibold text-teal-800">{child.title} overview</Link> : null}<NoteLinks notes={child.notes} /></section>)}<ParentPageFab href="/lab-img" /></div>; }

