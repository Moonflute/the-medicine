import * as T from 'three';import assert from 'node:assert/strict';import {AtlasScene} from './src/scene.js';
const mesh=new T.Mesh(new T.BoxGeometry(2,4,6),new T.MeshStandardMaterial());mesh.userData={partId:'test',organId:'heart'};const root=new T.Group();root.add(mesh);const scene={models:{heart:root},active:'heart',host:{dataset:{}},inspection:{mode:'all',part:'',organ:'',side:'',route:'',slice:true,cut:35},baseSide:''};
for(const [slicePlane,axis]of [['axial','y'],['coronal','z'],['sagittal','x']]){AtlasScene.prototype.inspect.call(scene,{slicePlane,sliceReverse:false});const a=mesh.material.clippingPlanes[0].clone(),point=a.coplanarPoint(new T.Vector3());AtlasScene.prototype.inspect.call(scene,{slicePlane,sliceReverse:true});const b=mesh.material.clippingPlanes[0];assert.equal(a.constant,-b.constant);assert.equal(a.normal.dot(b.normal),-1);assert.ok(Math.abs(b.distanceToPoint(point))<1e-9);const p=point.clone();p[axis]+=1;assert.ok(a.distanceToPoint(p)*b.distanceToPoint(p)<0);}console.log('Three actual scene clipping planes retain their cut position and invert the retained half-space');
